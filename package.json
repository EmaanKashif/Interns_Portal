{
  "name": "intern-portal",
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate --no-input || true"
  }
}